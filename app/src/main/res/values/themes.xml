<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme — extends Material3 with no action bar -->
    <style name="Theme.LocalLLM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme shown before Compose draws -->
    <style name="Theme.LocalLLM.Splash" parent="Theme.LocalLLM">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
